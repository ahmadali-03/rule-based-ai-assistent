# rule-based-ai-assistant
A deterministic, rule-based AI chatbot built with Python and React. Features input sanitization, O(1) dictionary lookups, token-level typo matching via difflib, and multi-intent keyword extraction. Includes both a standalone CLI terminal mode and a full-stack web app deployed on Vercel.
